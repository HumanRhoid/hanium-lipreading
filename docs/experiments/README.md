# 실험 기록 색인

립리딩 모델 학습 실험을 회차별로 기록한다. 파일명은 `YYYY-MM-DD-주제.md`다.

## 회차 목록

| 일자 | 문서 | 주제 | 결과 |
|---|---|---|---|
| 2026-08-15 | [교차검증-안정화기법](2026-08-15-교차검증-안정화기법.md) | label smoothing · grad clip · EMA 도입 | 평균 0.261 → **0.420** |
| 2026-08-16 | [시드-편차-측정](2026-08-16-시드-편차-측정.md) | 시드 3개 반복으로 신뢰구간 확보 | 중단 (데이터 구성 변경) |
| 2026-08-17 | [촬영방향-검증](2026-08-17-촬영방향-검증.md) | s06 재촬영 · s09 추가 | 평균 0.420 → **0.528** (단일 시드), 연령 가설 기각 |
| 2026-08-18 | [오답-구조-분석](2026-08-18-오답-구조-분석.md) | 앙상블 진단 · 혼동 구조 · 프레임 수 측정 | 앙상블 기각, **30프레임이 긴 문구 절반을 버림** |
| 2026-08-19 | [오차-분해](2026-08-19-오차-분해.md) | 45프레임 · 단방향 GRU · 8화자 오답 구조 | 둘 다 미확정, **예측이 소수 클래스로 쏠림** |
| 2026-08-19 | [프레임수-재검증](2026-08-19b-프레임수-재검증.md) | 60프레임 · 시간축 증강 · 오디오 교사 | 60프레임 +0.057 (s06 한정) |
| 2026-08-20 | [60프레임-8화자-확장](2026-08-20-60프레임-8화자-확장.md) | 8화자 24런 · 에폭 120 · 재현성 | **같은 시드가 같은 값을 안 낸다** (폭 0.09) |

## 판정 용어

이 프로젝트에서 세 가지를 구분해 쓴다. 2026-08-20에 정리했다.

```
채택    효과가 노이즈보다 크다고 확인됨
기각    효과가 없거나 반대 방향임이 확인됨
미확정  판정할 만큼 재지 못했다.  "효과 없음"이 아니다
```

**2026-08-20 이전 문서의 "기각"은 대부분 실제로는 미확정이다.** 노이즈 바닥을
재지 않고 판정선을 그었기 때문이다. 아래 목록은 그 기준으로 다시 분류한 것이다.

## 측정 정밀도

**같은 설정·같은 시드를 다시 돌려도 값이 다르다.** 2026-08-20에 확인했다.

```
s06 · 60프레임 · seed 42   세 번    0.809 / 0.739 / 0.822
s01 · 60프레임 · seed 42   두 번    0.675 / 0.764
s01 · 120에폭 3시드 평균    두 번    0.767 / 0.815
```

원인은 아직 확정되지 않았다. GPU 연산의 비결정성이 유력하나
`cudnn.deterministic`을 켜고 확인하지 않았다.

**조건-화자 한 칸 안의 3시드 표준편차가 0.0725다** (관측 다섯 칸).
여기서 판정력이 나온다.

```
화자  1명 · 3시드    2σ로 읽을 수 있는 최소 효과   0.118
화자  2명 · 3시드                                0.084
화자  4명 · 3시드                                0.059
화자  8명 · 3시드                                0.042
화자 16명 · 3시드                                0.030
```

**화자 8명이 최소 실험 단위이고, 그래도 +0.04 미만은 못 읽는다.**
교차검증 폴드는 학습 데이터를 공유하므로 실제 정밀도는 이보다 낮다.

## 현재 확정 설정

```python
epochs=80
batch_size=16
learning_rate=2e-4   # 시작값. 코사인으로 0까지 감소
hidden_dim=300
num_layer=2
dropout=0.3
weight_decay=0.01
label_smoothing=0.1
grad_clip=1.0
ema_decay=0.998
smoothing=3
augment=True
pretrained=False
```

전처리 상수는 `src/ml/preprocess/normalize.py`에 있다.
**`FIXED_FRAME_COUNT`은 30으로 둔다.** 실험은 60 위에서 돌리고 있으나 확정은 아니다.

60프레임의 독립 측정 세 번이 모두 양수였다. s06 3시드 +0.058, 8화자 seed42 +0.028,
8화자 3시드 +0.033. 규격 준수 6명만 보면 +0.052로 t=1.98이다. 방향은 일관되나
p는 0.05를 넘지 못한다. **화자 8명으로는 이 크기를 확정할 수 없다.**

## 데이터 현황

화자 8명 · 문구 15개 · 클립 1,234개

| 화자 | 성별 | 연령 | 촬영 | 원본 | 입술 crop | 세로배율 | 클립 |
|---|---|---|---|---|---|---|---|
| s01 | 남 | 젊음 | 세로 · 얼굴만 | 1440×1920 | 753×201 | ×0.40 | 157 |
| s03 | 여 | 젊음 | 세로 · 얼굴+상반신 | 1080×1920 | 384×116 | ×0.69 | 148 |
| s04 | 여 | 젊음 | 세로 · 얼굴+상반신 | 1080×1920 | 396×140 | ×0.57 | 150 |
| s05 | 남 | 젊음 | 세로 원거리 | **720×1280** | 196×61 | **×1.31** | 150 |
| s06 | 여 | 고령 | 세로 · 얼굴만 | 1440×1920 | 740×233 | ×0.34 | 157 |
| s07 | 남 | 고령 | 세로 · 얼굴만 | 1080×1920 | 552×156 | ×0.51 | 171 |
| s08 | 남 | 젊음 | 세로 · 얼굴만 | 1440×1920 | 684×280 | ×0.29 | 151 |
| s09 | 남 | 젊음 | 세로 원거리 | 1080×1920 | 260×77 | **×1.04** | 150 |

**세로배율은 crop을 모델 입력 높이 80px로 맞출 때의 배율이다. 1을 넘으면 확대가
일어나 이미지가 뭉개진다.** 촬영 가이드의 "입술 세로 80px 이상"이 이 기준이다.
현재 s05·s09가 미달이다.

crop 수치는 화자별 중간 프레임 1장 기준이다. 입이 벌어진 정도에 따라 세로가
흔들리므로 가로폭이 더 안정적인 지표다. s09는 120프레임 표본에서 **32%가 80px
미만**이었다(s09 외 화자는 0%).

화자별 성능(60프레임 · 80에폭 · 3시드 평균):

```
s06 0.784 · s04 0.769 · s01 0.741 · s08 0.667
s03 0.561 · s09 0.424 · s07 0.312 · s05 0.231
```

최상위와 최하위가 0.553 벌어진다. 30프레임 때는 0.473이었으나
그쪽은 단일 시드 값이라 직접 비교는 조심해야 한다.

s01·s06·s07은 초기 가로 촬영본을 세로로 재촬영한 데이터다.
s01·s07 구본은 `data/archive/`에 있다.

## 기각 — 효과가 없거나 반대임이 확인됨

- **모델 용량 과잉** — dropout 0.9로 train acc를 0.51까지 떨어뜨려도 val 불변
- **데이터 증강(공간)** — on/off 차이 0.001
- **앙상블** — 시드 3개 확률 평균으로 +0.004. 세 모델이 78.3%에서 같은 답을 낸다
- **연령이 화자 편차의 주된 축** — s06을 세로로 재촬영하자 0.320 → 0.732(+0.412).
  같은 화자 안의 비교라 노이즈보다 훨씬 크다
- **오디오를 직접 학습한 교사** — log-mel + 작은 CNN이 학습 0.923 / 검증 0.210.
  비디오와 같은 화자 과적합이며 더 심하다

위 다섯은 효과가 0에 가깝거나(0.001~0.004) 노이즈보다 훨씬 크다(0.412).
어느 쪽이든 판정이 안전하다.

- **ImageNet 사전학습** — 0.323 → 0.255(−0.068). **단일 측정이라 엄밀히는 미확정**이나,
  도메인이 다르고 방향이 음수라 우선순위를 낮게 둔다.
  **다만 립리딩 전용 사전학습과 혼동하면 안 된다**(미해결 과제 참조)

## 미확정 — 판정할 만큼 재지 못한 것

**아래는 전부 "효과 없음"이 아니라 "모른다"이다.**

| 개입 | 측정값 | 규모 | 표본 |
|---|---|---|---|
| 프레임 30 → 45 | −0.010 | 1승 2패 | s06 3시드 |
| 프레임 30 → 60 | +0.033 | 5승 3패 | 8화자 3시드 |
| 에폭 80 → 120 | **+0.041** | 7승 1패 | 8화자 3시드 |
| GRU 단방향 | −0.051 | — | 단일 시드 |
| 시간축 증강 | +0.021 | 2승 1패 | s06 3시드 |
| 예측 편향 보정 | +0.001 | — | 단일 측정 |

45프레임과 60프레임은 부호가 반대이므로 우선순위가 다르다. 60은 재검증할 값어치가
있고 45는 없다.

**에폭 80 → 120은 24런이 전부 끝났고 판정선에 0.001 못 미쳤다.**

```
2화자   +0.024   t 0.66
3화자   +0.044   t 1.70   p 0.127
5화자   +0.048   t 2.29   p 0.084
8화자   +0.041   t 2.97   p 0.021   7승 1패     판정선 0.042
```

짝 t검정으로는 유의하고(p 0.021) 사전 판정선으로는 미달이다. 판정선이
각 칸을 독립으로 가정한 보수적 근사이기 때문인데, 못 넘은 것을 넘었다고
바꿔 말하면 기준을 사후에 옮기는 것이 된다.

**판정을 미루는 진짜 이유는 따로 있다.** +0.041 중 얼마가 저장 기준
부풀림인지 모른다. s01만 보면 마지막 에폭 기준으로 다시 재자 +0.074가
+0.028로 줄었다. 80에폭 24런의 마지막 에폭 값을 wandb `val/acc` 요약에서
받아 양쪽을 같은 기준으로 다시 계산해야 한다. 재학습은 필요 없다.

**시드 편차가 줄었다는 5화자 시점의 기록은 철회한다.** 8화자 풀링
표준편차는 0.0720으로 80에폭의 0.0725와 사실상 같다(분산비 1.01).
s08 한 화자가 같은 설정에서 0.543 · 0.689 · 0.848을 냈다. **폭 0.305다.**

**화자 간 격차는 좁혀지지 않았다**(최고-최저 0.553 → 0.598). 크게 오른 것은
이미 잘하던 s01·s04·s06이고 하위 s05·s09는 +0.031과 +0.005에 그쳤다.

**학습 불안정이 병목인가** — 2026-08-18에 앙상블 결과(+0.004)를 근거로 "아니다"라고
판정했으나, 2026-08-20에 같은 설정 재실행이 0.09 흔들리는 것이 확인되어 **재개방한다.**

## 미해결 과제

### 측정과 방법론

- **같은 시드가 같은 값을 안 낸다** — 위 "측정 정밀도" 참조. 원인 미확정.
  `cudnn.deterministic=True`를 켜고 두 번 돌리면 갈린다. 약 40분
- **저장 기준이 검증 화자를 보고 고른다** — `train.py`가 검증 점수 최고점에서
  모델을 저장한다. 실사용에서는 새 사용자의 정답을 모르므로 불가능한 방식이다.
  마지막 에폭 값과 비교하면 s01 기준 **약 0.05 부풀려진다**(80에폭 0.007, 120에폭 0.053).
  **발표·서류 수치는 마지막 에폭 값을 써야 한다**
- **저장 기준의 부등호** — `train.py:365`가 `>=`라 동점이면 나중 에폭을 취한다.
  정점 직후는 하락 구간이라 항상 낮은 값을 고른다. `>` 로 바꾸면 해소된다
- **고정 에폭이 화자마다 안 맞는다** — 정점이 전체의 38%~100%에 흩어진다.
  에폭을 늘려도 안 풀렸다. 다만 이를 화자별로 맞추는 것은 위와 같은 이유로
  실사용에서 불가능하다

### 전처리에서 정보를 버리고 있다

- **입 벌어짐이 크롭에서 지워진다** — `margin=0.5`이 상자를 가로·세로 2배로 만들고,
  상자를 매 프레임 새로 계산한 뒤 매번 112×80으로 늘린다. 그래서 입술이 출력에서
  항상 56×40을 차지한다. 입을 두 배로 벌려도 화면에서 같은 크기로 보인다.
  **계산으로 유도한 것이고 실측하지 않았다.** 클립 하나에서 프레임별 크롭 세로
  길이를 출력해 openness와 같이 움직이는지 보면 확정된다
- **입력의 75%가 입술이 아니다** — 위와 같은 이유로 112×80 중 56×40만 입술이다
- **랜드마크 21점을 버린다** — `crop_lip`이 매 프레임 검출하고 상자만 쓴 뒤 좌표는
  반환하지 않는다. 좌표에는 크롭이 지운 입 벌어짐이 그대로 남아 있다.
  클립 단위로 정규화하면 얼굴 크기·위치가 지워져 화자 불변에 가까워진다
- **openness 시계열을 버린다** — `trim_to_speech`에 쓰고 폐기한다
- **발화 길이를 버린다** — 리샘플링이 모든 클립을 같은 프레임 수로 만든다.
  문구가 3~8음절로 다양한데 그 단서가 사라진다.
  `ClassificationHead`에 `padding_mask`가 있으나 놀고 있다
- **채널 3개 중 2개가 중복이다** — `to_grayscale_clahe`가 흑백으로 만든 뒤 3채널로
  복제한다. 주석에 적힌 이유(ResNet 사전학습 호환)는 사전학습이 기각되며 사라졌다.
  남는 두 채널에 프레임 차분을 넣으면 모델 변경 없이 움직임 정보를 줄 수 있다

### 모델과 학습

- **립리딩 전용 사전학습 미검토** — ImageNet 사전학습이 실패한 것은 도메인이 달라서다.
  립리딩 데이터로 사전학습된 시각 인코더는 축이 다르다. 우리처럼 화자 8명·클립 1234개
  규모에서 통상 쓰는 방법이다. **아직 조사하지 않았다**
- **오디오 증류** — Whisper 디코더에 15문구를 강제로 넣어 `log P(문구|오디오)`를 재고
  softmax로 분포화한다. 우리 데이터로 학습하지 않아 화자 유출이 불가능하고
  클립마다 다른 분포가 나온다. **교사 성능 0.93은 s06 클립 40개에서 잰 값이라
  표본이 작다.** 강제 디코딩이 우리 환경에서 되는지도 시험하지 않았다
- **label smoothing이 개별 검증된 적 없다** — 08-15에서 grad clip · EMA와 묶어
  한꺼번에 도입됐다(0.261 → 0.420). 개별 기여도를 분리한 적이 없다.
  증류와는 정면 충돌하므로(오답 14칸 균일 강제 vs 교사의 칸별 값) 어차피 정리해야 한다
- **검토 없이 굳은 기본값 다섯 개** — `bidirectional=True`, `>=` 동점 규칙,
  코사인 스케줄, `learning_rate=2e-4`, `label_smoothing=0.1`.
  `2e-4`는 배치 조정 때 선형 스케일링 규칙으로 정했는데, 그 규칙은 SGD 전제이고
  우리는 AdamW를 쓴다. Adam 계열에는 제곱근 스케일링이 통례다
- **warmup 미착수** — 2026-08-16이 제안했고 나흘째 그대로다

### 데이터와 오답 구조

- **화자 간 격차** — 최상위 0.784, 최하위 0.231. **화자 독립 시스템에서는 평균보다
  중요한 지표**인데 지금까지 평균만 보고 판단해 왔다
- **s05 재촬영** — 세로배율 ×1.31. 60프레임 3시드가 0.193/0.233/0.267로 **전부**
  30프레임 기준선 0.307보다 낮다. **데이터 품질이 알고리즘 개선을 막는 사례**다.
  s09(×1.04, 0.424)도 같은 축이나 덜 급하다
- **s07 원인 미상** — 촬영 조건이 정상(552×156, ×0.51)인데 0.312이다
- **예측이 소수 클래스로 쏠린다** — `숨쉬기힘들어요` 하나가 전체 오답의 21%를
  흡수하고, 7개 문구가 그쪽으로 일방통행한다(반대 방향 0회인 쌍이 7개).
  학습 데이터는 문구당 79~88개로 균형이 잡혀 있어 사전 확률 탓이 아니다.
  원인은 확인되지 않았다
- **혼동 구조가 화자마다 다르다** — 8화자의 1위 혼동이 하나도 겹치지 않는다.
  과반에서 반복되는 것은 `도와주세요 ↔ 토할거같아요`(6/8) 등 셋뿐이다
- **혼동 쌍은 전부 앞부분이 다르다** — 가래/화장실, 간호사/보호자, 더/추.
  **원리적으로 구분 불가능한 쌍은 아직 확인된 것이 없다.**
  문구 교체가 아니라 앞부분을 보게 만드는 문제다
  (2026-08-18 §3.2의 `물`/`불` 서술은 존재하지 않는 문구를 전제로 한 오류이며 철회됨)
- **실질 다양성은 120** — 클립 1,234개지만 화자 8 × 문구 15 = 120 조합이고
  조합당 10번은 거의 중복이다. 검증 쪽에서도 같은 문제가 나타난다 —
  검증 클립 157개의 통계적 힘이 독립 표본 약 32개어치다(이항분포 가정)
- **노트북 8-1 셀 결함** — 로컬 복사가 디렉터리 존재만 확인해, 화자를 추가해도
  옛 데이터로 학습이 돌 수 있다
- s06 구본(가로 촬영본)이 `data/archive/`에 보관돼 있지 않다

## 문서 작성 규칙

**회차 문서는 다음을 포함한다.**

1. 무엇을 바꿨고 왜 바꿨는지
2. 결과 수치와 비교 대상
3. 채택 · 기각 · 미확정 중 어느 것인지, 그 근거가 되는 표본 크기
4. 다음 회차로 넘기는 과제

**검증하지 않은 추론은 그렇다고 적는다.** 계산으로 유도한 것과 실측한 것을 구분한다.

**설정을 바꿀 때는 이 색인의 "현재 확정 설정"도 함께 갱신한다.**
